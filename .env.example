# Optional: Override the default port (3000)
PORT=3000

# Optional: Manually set the path to your browser executable
# (Only needed if the automatic detection fails)
# EDGE_PATH=C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
